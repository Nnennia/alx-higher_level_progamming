#!/usr/bin/python3
"""Function for object type"""


def is_same_class(obj, a_class):
    """Returns true is the object is the class"""
    if type(obj) == (a_class):
        return True
    return False
